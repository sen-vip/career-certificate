# v0.6.0 테스트 결과

## 개인정보 보호 기능

- 인쇄 실행 후 현재 발급 대상 선택 해제
- 주민등록번호 입력값 및 표시 여부 초기화
- 주소·성명·생년월일 등 현재 발급 화면 개인정보 초기화
- 시간강사·전문강사 방학기간 임시 입력값 초기화
- 업로드한 대장은 그대로 유지되어 다음 대상자 연속 발급 가능
- `대장 비우기` 실행 시 대장·사람 목록·선택 대상·검색어·현재 발급 개인정보 전체 초기화
- localStorage 사용처가 학교명·담당부서·담당자·전화번호·기본 용도 설정에 한정되는지 정적 확인
- sessionStorage, IndexedDB, cookie, fetch, XMLHttpRequest, sendBeacon, WebSocket 사용 코드 없음 확인

## 발급 도움말

- 상단 `? 발급 도움말` 버튼과 모달 연결 확인
- 기본 발급 6단계 안내 표시
- 기간제교원, 시간강사·전문강사, 교육공무직·일용직 안내 카드 표시
- 현재 앱에 없는 기능을 도움말에서 제공한다고 표시하지 않도록 대장 미등록 대상 안내 조정
- 개인정보 보호 및 원본자료 최종 확인 안내 표시
- ESC, 닫기 버튼, 배경 클릭으로 도움말을 닫을 수 있도록 이벤트 연결
- 모바일 폭에서 도움말 단계·카드가 1열로 전환되도록 스타일 확인

## 보안 의존성 및 정책

- 기존 jsDelivr SheetJS 0.18.5 참조 제거
- SheetJS 공식 배포처 0.20.3 참조 확인
- CSP `connect-src 'none'` 적용으로 앱 코드의 데이터 통신 경로 차단
- 외부 스크립트 요청에 `referrerpolicy="no-referrer"` 적용

## 회귀 정적 검사

- `node --check app.js` 문법 검사 통과
- `index.html` HTML parser 구문 검사 통과
- 화면 제목과 버전 배지 `v0.6.0` 일치
- 기본양식 3종 파일 유지
- 일반·기간제교원·시간강사 관련 기존 파싱·미리보기·인쇄 함수 유지
- 기존 A4 인쇄 CSS 및 증명서 생성 구조는 수정하지 않음
