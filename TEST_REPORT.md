# v0.5.2 테스트 결과

## 기능 확인

- 시간강사·전문강사 경력별 방학기간 설정 기능 유지
- 방학기간을 3건 이상 연속 추가·삭제할 수 있는지 확인
- 각 기간에 `구분 표시 안 함`, `여름방학`, `겨울방학`, `학년말·봄방학`, `기타` 선택 확인
- 방학 구분 기본값은 `구분 표시 안 함`이며 현재 발급에만 적용
- 방학기간 카드에 입력 가능한 경력 시작일·종료일 표시

## 연도 오타 및 오류 검증

- 경력기간 `2017-03-03 ~ 2018-01-31`에서 종료일 `2028-01-10` 입력 시 범위 오류 감지
- 월·일을 유지한 유일 후보 `2018-01-10` 빠른 수정 버튼 표시 확인
- 빠른 수정 버튼을 실제 마우스로 눌렀을 때 값이 즉시 교체되고 해당 오류가 해제되는지 확인
- 날짜 입력칸의 포커스 이동으로 버튼이 사라지지 않도록 포인터 동작 확인
- 수정 후보가 둘 이상이거나 경력기간 안에 후보가 없으면 임의 제안하지 않음
- 오류 문구에 잘못 입력한 날짜와 경력 시작일 또는 종료일을 함께 표시
- 날짜 누락, 존재하지 않는 날짜, 기간 역전, 중복·겹침 검증 유지

## 부분 미리보기 확인

- 방학기간 1이 정상이고 방학기간 2가 오류인 경우 정상 기간 1을 계속 표시
- 오류 기간의 안내는 정상 기간과 분리해 빨간 확인 문구로 표시
- 오른쪽 증명서 미리보기에도 정상 기간만 반영되는지 확인
- 오류가 남아 있는 동안 인쇄 버튼 실행 시 차단되고 첫 오류 입력칸으로 이동

## 출력 및 디자인 확인

- 방학 구분을 선택하면 `여름방학 2017. 6. 1. ~ 2017. 7. 30.` 형태로 출력
- 구분 표시 안 함이면 기존처럼 날짜만 출력
- 방학기간이 여러 건이어도 전용 안내 영역에서 자연스럽게 줄바꿈
- 모바일 390px에서 구분 선택, 날짜 입력, 빠른 수정 버튼이 가로 넘침 없이 표시
- A4 미리보기의 학교장명·담당자 영역과 방학기간 안내가 겹치지 않는지 확인

## 정적 검사

- `node --check app.js` 문법 검사 통과
- 화면 제목과 버전 배지 `v0.5.1` 일치
- 기본양식 3종과 기존 일반·기간제교원·시간강사 기능 파일 유지


## v0.5.2 추가 점검

- 기간제교원 기본양식 임용시작일에 `20120301` 입력 시 `######` 대신 숫자가 정상 표시되는지 확인
- 숫자형 `20120301`을 업로드 날짜 `2012-03-01`로 변환하는 로직 확인
- 기존 Excel 날짜 일련번호는 기존 방식대로 날짜로 인식하는지 확인
