# v0.4.1 테스트 결과

## 수정 내용 확인

- 대장 수정창의 시작일·종료일을 네이티브 `date` 입력에서 숫자 입력용 텍스트 필드로 변경
- 숫자만 연속 입력해도 연·월·일 구분자가 자동 적용되도록 처리
- 달력 버튼과 보조 날짜 선택기를 연결해 기존 달력 선택 기능 유지
- 대장 저장 시 기존 `parseDateStrict()` 검증을 그대로 사용하도록 확인

## 날짜 입력 단위 검사

- `20260806` → `2026-08-06` 자동 변환
- `2026.08.06.` → `2026-08-06` 자동 변환
- `2026-8-6` → `2026-08-06`으로 저장 전 정규화
- `20260231` → 유효하지 않은 날짜로 판정
- 빈 종료일 → 기존 종료일 누락 오류 유지
- 달력에서 선택한 값 → 보이는 입력칸에 동일한 `YYYY-MM-DD` 값 반영

## 정적 검사

- JavaScript `node --check` 통과
- 화면 제목과 버전 배지 `v0.4.1` 일치
- 시작일·종료일 입력칸 ID와 기존 저장·오류 포커스 연결 유지
- 날짜 입력 버튼은 `type="button"`으로 설정되어 저장 동작과 충돌하지 않음

## 기존 기능 회귀 확인 항목

- 생년월일 누락은 신청자 공통정보 안내로 처리
- 종료일·근무부서 등 개별 경력 오류는 해당 대장 수정창으로 연결
- 대장 수정 모달 1열 배치와 고정 제목·저장영역 유지
- 일반·기간제교원·시간강사별 조건부 입력항목 유지
- 경력 선택, 증명서 미리보기, 수정자료 엑셀 저장 로직 변경 없음

브라우저 자동화 실행 환경의 로컬 페이지 접근 제한으로 실제 클릭 자동화는 수행하지 못했으며, 코드 정적 검사와 날짜 변환 단위 검사를 수행했습니다.
