# v0.4.0 테스트 결과

## 정적 검사

- JavaScript `node --check` 통과
- 화면 제목과 버전 배지 `v0.4.0` 일치
- 기본양식 3종 실제 파일명과 `index.html` 다운로드 링크 일치
- 수정 모달 입력폼이 `edit-form-grid` 1열 구조로 구성됨
- 개인정보 원문을 출력하는 콘솔 로그 없음

## 오류 이동·수정 브라우저 테스트

일반 대장 체험자료의 생년월일과 종료일이 비어 있는 대상자를 기준으로 확인했습니다.

- 생년월일 안내가 경력 건수와 관계없이 한 번만 표시됨
- 생년월일 누락 자체는 경력 카드의 오류 건수에 포함되지 않음
- 종료일 오류는 해당 경력 카드에만 오류 배지로 표시됨
- 상단 종료일 오류를 누르면 해당 일반 경력 수정 모달이 열림
- 수정 모달이 열린 뒤 종료일 입력칸으로 자동 스크롤·포커스됨
- 오류 입력칸에 빨간 테두리와 구체적인 안내문이 표시됨
- 수정 모달 입력폼의 계산된 열 수가 1열임
- 모달 제목 영역과 저장 버튼 영역이 고정되고 본문만 스크롤됨
- 오류 수정 후 저장하면 모달이 닫히고 경력 선택 체크박스가 활성화됨
- 수정 후 남은 안내는 신청자 생년월일 한 건만 표시됨
- 오류가 남은 상태로 저장하면 모달이 닫히지 않고 남은 오류 입력칸에 다시 포커스됨
- 생년월일 안내를 누르면 발급 정보의 `field-birth` 입력칸으로 이동·포커스됨
- 신청자 생년월일을 입력하면 상단 안내가 즉시 사라짐
- 기간제교원 오류 클릭 시 종료일 입력칸 포커스와 과목 입력항목 노출 확인
- 시간강사 수정창의 직위구분 등 조건부 항목과 1열 배치 확인
- 390px 모바일 화면에서 모달 좌우 이탈 없음 및 하단 저장영역 노출 확인

## 회귀 확인

- 일반 대장 체험자료 4명·경력 5건 표시
- 생년월일 누락 외 날짜 역전·종료일 누락 오류 차단 유지
- 정상 경력 전체 선택과 개별 선택 기능 유지
- 발급 정보의 생년월일 인쇄 전 필수검증 유지
- 대장 수정창에서 생년월일을 입력하지 않아도 다른 경력 오류만 해결되면 저장 가능
- 일반·기간제교원·시간강사별 조건부 입력항목 유지
- 주민등록번호 직접입력 자료는 대상자 변경·초기화 시 삭제되는 기존 동작 유지

## 이전 버전 핵심 회귀

- 시간강사·전문강사, 기간제교원, 교육공무직·일용직 대장 인식 구조 유지
- `900101` 생년월일 인식 로직 유지
- 엑셀 날짜 일련번호의 하루 당김 방지 로직 유지
- 5줄·10줄 및 11건 이상 별지 출력 구조 유지
- 주민등록번호 표시 여부와 직접입력 검증 유지
