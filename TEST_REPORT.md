# v0.7.2 시간강사 주당 수업시수 필수화 확인

- 시간강사 + 주당 수업시수 없음 → 오류 경력으로 분류되어 선택/인쇄 불가
- 시간강사 + 숫자 8 → 증명서 경력행에 `시간강사(주당 8시수)`로 출력
- 시간강사 + `12시수` → `주당 12시수`로 출력
- 시간강사 + `6시간` → 기존 자료 호환을 위해 `주당 6시간`으로 출력
- 여러 시간강사 경력은 각 레코드의 주당 수업시수를 독립적으로 매칭
- 전문강사 주당 수업량 누락은 필수 오류로 승격하지 않음
- 방학기간 표시 옵션은 시수와 별도 상태로 유지

---

# v0.7.1 UI 핫픽스 확인

- 도움말 CSS의 잘못된 이스케이프 문자 제거 및 스타일 정상 적용 확인
- 도움말 dialog 구조/닫기 버튼 ID·이벤트 연결 유지 확인
- 기존 하단 푸터 제거, 제작자 `senvip(학돌랩)` 상단 브랜드 영역 및 도움말 하단 표기 확인
- app.js 구문 검사 통과, styles.css 중괄호 균형 확인

---

# v0.7.0 안정화 점검

- [x] `node --check app.js` JavaScript 구문 검사 통과
- [x] 교육공무직 기본양식 헤더 점수: general 20 / teacher 10 / instructor 6으로 교육공무직 우선 판별 확인
- [x] 기간제 기본양식 헤더 점수: teacher 11 / general 8로 기간제 우선 판별 확인
- [x] 시간강사 기본양식 헤더 점수: instructor 17로 시간강사 우선 판별 확인
- [x] 직위 기본값만 있는 기간제 준비행이 미사용 행으로 제외되는 로직 확인
- [x] 교육공무직 `임용시작일·임용종료일`이 일반 경력 날짜로 정상 정규화되는 로직 확인
- [x] 완전 중복 경력의 두 번째 행이 `error`로 분류되는 것 확인
- [x] `2022-02-01~03-04` / `2022-03-01~2023-01-31` 경력 겹침을 4일 `error`로 감지 확인
- [x] 생년월일 미입력 시 인쇄 차단 확인
- [x] 확인 필요 경력은 담당자 확인 체크 전 인쇄 차단, 확인 후 허용 확인
- [x] 시간강사 상세 방학기간이 비어 있으면 인쇄 차단 확인
- [x] HTML의 정적 id 참조 누락 없음 확인

# v0.6.6 추가 점검

- [x] 포상 종류 긴 문구 자동 줄바꿈
- [x] 시행청/처분청 긴 문구 자동 줄바꿈
- [x] 징계 종류 및 직위해제 사유 긴 문구 자동 줄바꿈
- [x] 다건 입력 행 높이 자동 확장
- [x] 매우 긴 문구 단계별 폰트 축소 클래스 적용
- [x] 상벌 표 colgroup 폭 배분 확인

# v0.6.5 추가 점검

- 시간강사 주당 수업량 누락 시 모달이 뜨지 않는지 확인
- 누락 경력이 `확인 필요`로 표시되고 클릭 시 대장 수정창의 주당 수업량 필드로 이동하는지 확인
- 누락 상태에서도 경력 선택·미리보기·인쇄가 가능한지 확인
- 포상·징계·직위해제 각 0건/1건/2건 이상 입력 시 미리보기 표 행이 정상 생성되는지 확인
- 반복 입력 중 빈 항목이 있으면 인쇄 전 확인 메시지가 표시되는지 확인

# v0.6.5 테스트 결과

- 포상 기본 `해당없음` 및 있음 선택 시 연월일·종류·시행청 표시 확인
- 징계 기본 `해당없음` 및 있음 선택 시 연월일·종류·시행청 표시 확인
- 직위해제 기본 `해당없음` 및 있음 선택 시 연월일·사유·처분청 표시 확인
- 있음 선택 후 세부항목 누락 시 인쇄 전 확인 메시지 표시
- JavaScript 구문 검사 통과

# v0.6.2 테스트 결과

- `app.js` JavaScript 구문 검사 통과 (`node --check`)
- 시간강사 담당구분 누락/비표준 값이 `error`를 만들지 않는 것 확인
- 시간강사 주당수업시간 미기재가 `error`를 만들지 않는 것 확인
- 담당내용·숫자 형식 보조정보는 `warning`만 사용하여 경력 선택·미리보기를 차단하지 않음
- 기본양식 확인메모 중 `담당구분 확인`, `담당내용 확인`, `주당수업시간 확인`, `총주수 확인`은 발급 경고에서 제외
- 수정창 담당구분에 `선택 안 함` 추가 및 임의 `교과` 기본선택 제거
- 버전 표기 v0.6.2 갱신 확인

# v0.6.1 테스트 결과

## 핵심 시나리오

1. 기존 경력 `2012-03-01 ~ 2013-02-28` + 별도 행 `종료일 2012-07-17`만 존재
   - 퇴직 발령 후보로 감지하도록 로직 추가
   - 이전 경력기간 안에 날짜가 들어오는 경우에만 연결 후보 생성

2. 기존 경력 `2012-03-01 ~ 2013-02-28` + 별도 행 `시작일 2012-07-17`만 존재
   - 시작일 칸에 날짜가 있어도 같은 방식으로 감지

3. 퇴직사유 미확인
   - `이전 경력의 퇴직으로 반영` 버튼 비활성화
   - 의원면직 등 사유를 선택한 뒤에만 반영 가능

4. 퇴직 발령 아님
   - 별도 경력행으로 유지
   - 이미 값이 있는 쪽의 반대 날짜칸으로 수정 포커스 이동

5. 미확인 후보가 선택 경력에 연결된 상태
   - 인쇄 전에 확인 안내가 나오도록 검증 추가

## 정적 검증

- `node --check app.js` 통과
- 후속 발령 추론, 사유 선택, 반영/비반영 이벤트 연결 확인
- 버전 표기 v0.6.1 갱신 확인

# v0.6.0 테스트 결과

## 기능 확인

- 시간강사·전문강사 경력별 방학기간 설정 기능 유지
- 방학기간을 3건 이상 연속 추가·삭제할 수 있는지 확인
- 각 기간에 `구분 표시 안 함`, `여름방학`, `겨울방학`, `학년말·봄방학`, `기타` 선택 확인
- 방학 구분 기본값은 `구분 표시 안 함`이며 현재 발급에만 적용
- 방학기간 카드에 입력 가능한 경력 시작일·종료일 표시

## 연도 오타 및 오류 검증

- 경력기간 `2017-03-03 ~ 2018-01-31`에서 종료일 `2028-01-10` 입력 시 범위 오류 감지
- 월·일을 유지한 유일 후보 `2018-01-10` 빠른 수정 버튼 표시 확인
- 빠른 수정 버튼을 실제 마우스로 눌렀을 때 값이 즉시 교체되고 해당 오류가 해제되는지 확인
- 날짜 입력칸의 포커스 이동으로 버튼이 사라지지 않도록 포인터 동작 확인
- 수정 후보가 둘 이상이거나 경력기간 안에 후보가 없으면 임의 제안하지 않음
- 오류 문구에 잘못 입력한 날짜와 경력 시작일 또는 종료일을 함께 표시
- 날짜 누락, 존재하지 않는 날짜, 기간 역전, 중복·겹침 검증 유지

## 부분 미리보기 확인

- 방학기간 1이 정상이고 방학기간 2가 오류인 경우 정상 기간 1을 계속 표시
- 오류 기간의 안내는 정상 기간과 분리해 빨간 확인 문구로 표시
- 오른쪽 증명서 미리보기에도 정상 기간만 반영되는지 확인
- 오류가 남아 있는 동안 인쇄 버튼 실행 시 차단되고 첫 오류 입력칸으로 이동

## 출력 및 디자인 확인

- 방학 구분을 선택하면 `여름방학 2017. 6. 1. ~ 2017. 7. 30.` 형태로 출력
- 구분 표시 안 함이면 기존처럼 날짜만 출력
- 방학기간이 여러 건이어도 전용 안내 영역에서 자연스럽게 줄바꿈
- 모바일 390px에서 구분 선택, 날짜 입력, 빠른 수정 버튼이 가로 넘침 없이 표시
- A4 미리보기의 학교장명·담당자 영역과 방학기간 안내가 겹치지 않는지 확인

## 정적 검사

- `node --check app.js` 문법 검사 통과
- 화면 제목과 버전 배지 `v0.5.1` 일치
- 기본양식 3종과 기존 일반·기간제교원·시간강사 기능 파일 유지


## v0.5.2 추가 점검

- 기간제교원 기본양식 임용시작일에 `20120301` 입력 시 `######` 대신 숫자가 정상 표시되는지 확인
- 숫자형 `20120301`을 업로드 날짜 `2012-03-01`로 변환하는 로직 확인
- 기존 Excel 날짜 일련번호는 기존 방식대로 날짜로 인식하는지 확인


## v0.6.0 추가 점검

- 기간제교원 기본양식 임용시작일·임용종료일 셀은 `General` 서식으로 확인
- 기간제교원 생년월일/주민번호 셀은 텍스트 서식으로 확인
- 시간강사 및 공무직 기본양식에도 동일 원칙 적용
- 생년월일 `19900101` → 1990-01-01 파싱 확인
- 생년월일 `900101` → 1990-01-01 파싱 확인
- JavaScript 문법 검사 통과


## v0.6.4 추가 점검

- 주당 수업량 누락 경력 선택 시 안내창 호출
- `6시간` 입력 → `주당 6시간` 표시
- `8시수` 입력 → `주당 8시수` 표시
- `입력 없이 계속` 선택 시 경력 선택·미리보기 유지
- 수정본 엑셀 내보내기 시 `시간`/`시수` 원문 유지
